db-config {
  jdbc-url = "jdbc:postgresql://localhost:5433/postgres"
  user = "postgres"
  password = "example"
  max-pool-size = 10
  timeout = 30000
}

web-config {
  host = "0.0.0.0"
  port = 8080
}